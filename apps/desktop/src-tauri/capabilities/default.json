{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — core + local SQLite via the sql plugin",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default"
  ]
}