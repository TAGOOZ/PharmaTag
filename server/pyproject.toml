[project]
name = "pharmatag-core"
version = "0.1.0"
description = "PharmaTag core server — FastAPI monolith (schema-first; ticket T01)"
requires-python = ">=3.12"
dependencies = [
    "SQLAlchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.2",
]

[tool.alembic]
script_location = "alembic"